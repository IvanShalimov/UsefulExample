<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ivan.secondareatest">

    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".other.mp4parser.MainActivity" />
        <activity android:name=".other.mp4parser.TrimMp4Activity" />
        <activity android:name=".other.mp4parser.Mp4ParserActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".CurrentLocationMapActivity"
            android:label="@string/title_activity_current_location_map" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".example.MarksMapActivity"
            android:label="@string/title_activity_marks_map" />
        <activity
            android:name=".example.SneakMapActivity"
            android:label="@string/title_activity_sneak_map" />
        <activity
            android:name=".example.KmShowMapsActivity"
            android:label="@string/title_activity_km_show_maps" />
        <activity
            android:name=".example.CircleRotateMapActivity"
            android:label="@string/title_activity_circle_rotate_map" />
        <activity
            android:name=".example.ClusteringMapActivity"
            android:label="@string/title_activity_clustering_map" />
        <activity
            android:name=".example.HeatMapActivity"
            android:label="@string/title_activity_heat_map" />
        <activity android:name=".example.MapViewActivity" />
        <activity android:name=".example.zoom.ZoomMapActivity"></activity>
    </application>

</manifest>